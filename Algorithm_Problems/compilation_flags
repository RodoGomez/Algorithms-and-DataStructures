In order to compile all the solutions provided, use the following compilation flags:

C compilation:
gcc -pipe -O2 -std=c11 <filename> -lm

C++ compilation:
g++ -pipe -O2 -std=c++14 <filename> -lm

The Python version used is Python3.
